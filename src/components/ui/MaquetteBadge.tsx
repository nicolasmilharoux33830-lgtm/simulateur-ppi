export default function MaquetteBadge() {
  return (
    <div className="fixed bottom-4 left-4 z-50 px-3 py-1.5 bg-gold text-pine text-xs font-display font-semibold rounded-full shadow-lg border border-gold/50" role="note" aria-label="Avertissement : document sans caractère officiel">
      Maquette — sans caractère officiel
    </div>
  );
}
